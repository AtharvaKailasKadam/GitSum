# GitSum Frontend Configuration

# Path pointing to your Node.js/Express backend API.
# Defaults to /api for local development (which Vite proxies to port 3001)
# and for production on Vercel (using serverless function routing).
VITE_API_BASE_URL=/api
